# Decks ![Downloads](https://img.shields.io/github/downloads/dscherdi/decks/total) ![Downloads Latest](https://img.shields.io/github/downloads/dscherdi/decks/latest/total?style=flat-square) ![Release](https://img.shields.io/github/v/release/dscherdi/decks)

[English](./README.md) · [Deutsch](./README.de.md) · [Español](./README.es.md) · [Français](./README.fr.md) · [Italiano](./README.it.md) · [Русский](./README.ru.md) · [Türkçe](./README.tr.md) · [Shqip](./README.sq.md) · [العربية](./README.ar.md) · [हिन्दी](./README.hi.md) · [中文](./README.zh.md) · **繁體中文** · [日本語](./README.ja.md)

**將您的 Obsidian 筆記變成閃卡 (Flashcards)。無需特殊語法，無需建立單獨的牌組。**

只需用 `#decks` 標記一個檔案。每個 `##` 標題都會變成卡片的正面；其下方的文字會變成背面。表格、圖片遮罩 (Image occlusion) 和 `==填空==` (Cloze) 也能以同樣的方式運作。卡片複習排程由 FSRS-6 —— 現代的間隔重複演算法來處理。

![Demo](./decks_showcase.gif)

[Discord](https://discord.com/channels/686053708261228577/1497268419861418035) · [版本說明](./release-notes/) · [贊助我一杯咖啡](https://www.buymeacoffee.com/dscherdil0)

## 為什麼選擇 Decks

- **您的筆記就是牌組。** 標記一個檔案，所選層級的每個標題都會變成正面，其下方的文字變成背面。如果您習慣使用 Anki，那麼在這裡您不需要重複編寫兩次。
- **四種格式，無需學習語法。** 標題、雙列表格、圖片遮罩以及您已經在使用的 `==填空==` 標記。
- **考試模式。** 以選擇題、輸入題和填空題進行的計分考試。
- **FSRS 原生排程。** 三種設定檔（標準 / 高強度 / 經過訓練），可按標籤設定記憶留存率目標，徹底告別舊版 SM-2 演算法。
- **演算法微調。** 一鍵最佳化工具可根據您自己的複習歷史訓練 FSRS 權重 —— 為您的遺忘曲線提供更好的複習排程，全部在本地端完成。
- **真正的多裝置同步。** 資料庫透過 iCloud/Dropbox 自動合併 —— 在手機和電腦上複習，絕不遺失歷史記錄。
- **專為行動裝置打造。** 觸控最佳化的複習介面，適配安全區域 (safe-area)，每天都在手機上進行測試。

## 60秒快速入門

1. 從社群外掛程式中安裝 **Decks** 並啟用。
2. 打開任意筆記。在其 frontmatter 或內文中新增 `#decks` 標籤。
3. 寫一個 `##` 標題，然後在其下方寫一段內文。需要多少張卡片就重複多少次：

   ```markdown
   ---
   tags: [decks/english]
   ---

   ## "Hola" 是什麼意思？

   你好 (Hello)。

   ## 怎麼用英文說 "謝謝"？

   Thank you.
   ```

4. 點擊側邊欄中的 **大腦圖示** 打開 Decks 面板。點擊您的檔案以開始複習。

檔案名稱將成為牌組名稱。卡片會在您儲存筆記時自動同步。

## 卡片格式

Decks 支援四種編寫卡片的方式。選擇最適合您記錄筆記習慣的一種。

<details>
<summary><b>標題 + 段落</b> — 預設格式。所配置層級（預設 H2）的每個標題都是正面，內文下方是背面。</summary>

```markdown
---
tags: [decks/english]
---

## "Hola" 是什麼意思？

你好。

## 怎麼用英文說 "謝謝"？

Thank you.
```

檔案名稱即為牌組名稱。標題層級可以按設定檔進行更改（預設 H2）。高於所配置層級的標題不會變成卡片 —— 它們會作為麵包屑路徑（例如 `第 1 章 > 第 2 節`）附加到每張卡片上以提供上下文。

可為「標題+段落」卡片新增可選的**備註**：在內文任意位置寫一則 Obsidian 註解（`%%提示%%`），或在結尾的 `---` 分隔線之後書寫。複習時可依需求（**N** 鍵）顯示備註。

</details>

<details>
<summary><b>表格</b> — 雙列 Markdown 表格，帶有一個選填的筆記欄。</summary>

```markdown
## 核心概念

| 問題             | 答案                   |
| ---------------- | ---------------------- |
| 什麼是光合作用？ | 植物用來轉化陽光的過程 |
| 解釋一下重力     | 吸引物體相互靠近的力   |
```

- 第一欄 = 正面，第二欄 = 背面。標題列將被忽略。
- 表格必須直接位於標題下方（內部不能有其他段落）。
- 新增第三個「筆記」欄，用於在複習期間透過按 **N** 鍵切換顯示的提示或記憶法。

</details>

<details>
<summary><b>填空 (Cloze)</b> — 使用 <code>==文字==</code> 語法來挖空。</summary>

```markdown
## 太陽系

==太陽==是我們太陽系中心的恆星。距離最近的行星是==水星==，最大的行星是==木星==。
```

每個標記的部分都會成為一張獨立的卡片。在複習時，活動的填空將顯示為 `[...]`；點擊即可顯示。填空也可以在表格儲存格內運作。預設為啟用。

</details>

<details>
<summary><b>圖片遮罩 (Image occlusion)</b> — 遮擋圖片的區域並回憶其下的內容。兩種方式：互動式（繪製方框）或編號列表。</summary>

**互動式（推薦）。** 執行命令 **「在游標處建立圖像遮擋」**，選擇一張圖片，然後直接在編輯器中於圖片上繪製方框。為每個方框輸入 Markdown/LaTeX 答案，或留空以僅遮擋圖片中已有的標籤。它會儲存為一個自包含的 `decks-occlusion` 程式碼區塊（座標為百分比，因此可在任何裝置上自適應）：

````markdown
```decks-occlusion
image: "[[heart.png]]"
version: 2
masks:
  - id: m1
    x: 12.5
    y: 30
    w: 18
    h: 9.5
    answer: "左**心室**"
  - id: m2
    x: 55
    y: 22
    w: 14
    h: 8
    answer: ""
```
````

每個方框都是一張卡片。複習時，方框在正面被遮擋，在背面（連同答案）被揭示；在閱讀檢視中你會看到完整標註的圖示。可隨時透過程式碼區塊的**編輯**按鈕或卡片管理器進行編輯。

**編號列表（簡單）。** 嵌入圖片後跟一個編號列表同樣有效：

```markdown
## 手臂骨骼

![[arm_bones.png]]

1. ==肱骨==
2. ==橈骨==
3. ==尺骨==
```

列表中的每個項目都是一張卡片；圖片顯示在正面，對應的項目在背面被挖空。

兩者都建立在填空功能之上，因此設定檔中必須啟用填空，且區塊必須位於被解析的標題之下。

</details>

### Canvas 牌組

在 Obsidian Canvas (`.canvas`) 檔案中編寫卡片，而非 Markdown 檔案。已設定資料夾中的每個 canvas 都會成為一個牌組；每個文字節點都會以上述相同的四種卡片格式進行解析。透過 **設定 → Canvas 牌組** 設定：資料夾與標籤（預設 `#decks/canvas`）。在複習中點選「前往來源」會開啟 canvas 並聚焦到來源文字節點。首次安裝（或升級）時，會在 `Canvas decks/` 資料夾中自動建立一份 `Decks — Canvas 入門.canvas`。

**空間卡片（Spatial cards）**：用連線連接文字節點，每條連線都會變成一張卡片——起始節點是正面（問題），終點節點是反面（答案），連線上的標籤作為提示。支援鏈式連接（A → B → C）、一對多、多對一；未連接的節點仍按上述四種格式解析。詳情請見 **[docs/CANVAS_DECKS.md](docs/CANVAS_DECKS.md)**。

![Canvas Spatial Cards Demo](./canvas_spatial_cards_demo.gif)

## 範本

用你只需設計一次的卡片樣式來渲染表格的每一列。以 HTML/CSS 或 Markdown 編寫，放入 `{{Column}}` 佔位符，再用標籤將它
綁定到表格——一個範本即可為每一個相符的列設定樣式。

```decks-html-front
<ruby>{{Word}}<rt>{{Reading}}</rt></ruby>
```

在 **設定 → 範本** 中指定一個資料夾，為範本檔案與表格的標題加上相同的標籤即可。範本支援 HTML 或 Markdown 的正面/背面/筆記
三個面，在經過淨化、感知主題的沙箱中渲染，並提供 CSS 變數（`--padding`、`--align`、`--bg` 等）以完全掌控版面——從舒適的
閱讀卡片到鋪滿整張卡片的自訂設計。沒有相符範本的表格仍使用一般的欄位。

完整指南與範例請見 **[docs/TEMPLATES.md](docs/TEMPLATES.md)**。

## 考試牌組

將牌組作為計分考試來進行：抽出一組題目，在一次作答中以任意順序完成，結束時提供成績明細——並可選擇設定時間限制與及格分數。考試按設定檔啟用，並新增一種編寫格式：標題後接一個任務清單即成為一張選擇題卡片。

```markdown
## 哪一種元素是惰性氣體？

- [ ] 氧
- [x] 氬
- [ ] 氮
```

`- [x]` 標記正確選項；勾選多個即為多選題。

- 為筆記加上牌組標籤的 `exams` 子標籤（預設 `#decks/exams`）即可使用預裝的 **Exams** 設定檔，或在任意設定檔中開啟 **考試題目**。
- 從牌組選單（**⋮ → 開始考試**）或點選考試牌組開始；設定對話框會顯示題目數量，並讓你調整考試設定。
- 除選擇題外，「標題+答案」卡片與表格列會作為輸入題出題，填空卡片則顯示句子，並將標記處變成需要輸入作答的空格。
- 輸入的答案可精確判分、容忍小的拼寫錯誤，或自行評分；考試預設值（題目數量、時間限制、及格分數、亂序、回饋時機、選項標籤）保存在設定檔中。
- 已完成的考試會儲存在外掛資料庫中，可跨裝置合併，並包含在備份中。

首次安裝時會建立一個展示所有題型的「考試示範」牌組（也可透過 **「建立考試示範牌組」** 命令建立）。

完整的編寫規則請見 **[docs/EXAM_DECKS.md](docs/EXAM_DECKS.md)**。

## 個人化排程

FSRS 附帶了開箱即用的合理預設設定。一旦您累積了約 100 次複習記錄，就可以根據您自己的複習歷史來訓練演算法的 21 個權重，並獲得為您特定遺忘曲線量身打造的卡片排程 —— 這與 Anki 電腦版的作用一樣，但完全在本地端運行，沒有伺服器，也沒有資料遙測。

## 從 Spaced Repetition 遷移而來

已經在使用 **Spaced Repetition** 外掛了嗎？你可以切換到 Decks，**而不會遺失任何卡片或複習紀錄**——並且能夠從你上次停下的地方繼續複習。

從卡組面板工具列開啟遷移工具（立方體圖示），或執行 **「Migrate from Spaced Repetition plugin」** 命令。

**你的原始筆記永遠不會被更動。** 遷移採用附加方式：它會將新檔案寫入你選擇的目標資料夾（沿用你的結構），並讓來源筆記維持原樣。重新執行遷移工具只會覆寫它先前產生的檔案。

**運作方式**

1. **選擇要掃描的來源資料夾**（或留空以掃描整個保管庫），以及輸出用的目標資料夾。Decks 會找出每一篇含有舊式卡片的筆記——單行（`Front :: Back`）、反向（`Front ::: Back`）、多行（`?` / `??`）、填空（`==…==` / `{{…}}`）——以及整篇筆記複習（`#review` 標籤）。
2. **每篇筆記會被拆成兩個乾淨的檔案。** 一個 **閃卡卡組**（`<筆記> (閃卡)`）以標準 Decks 格式保存擷取出來的卡片，而一篇 **可閱讀的筆記** 則保留正文——卡片語法會被 *還原* 成一般文字（`::` / `:::` 變成「 — 」，`?` / `??` 將問題與答案合併，而 `==…==` / `{{…}}` 填空則還原為其答案）。你設定的分隔符會被沿用，因此即使你自訂過也能正常運作。不會刪去任何內容——你的閱讀筆記維持完整。
3. **這兩個檔案會在 frontmatter 中互相連結。** 可閱讀的筆記會取得一個指向其卡組的 `閃卡` 屬性，以及一個指回原始筆記的 `來源筆記` 屬性；卡組也會取得一個 `來源筆記` 屬性。如果筆記已經使用了其中某個屬性名稱，遷移工具會覆寫它，而非建立重複項。你的標籤會被保留——舊式的基礎標籤（例如 `#flashcards`）會被轉換成你設定的 Decks 標籤。
4. **反向卡片會變成兩張卡片。** 一張 `Front ::: Back` 卡片會在 **同一個** 卡組檔案中展開為一張正向卡片與一張對調卡片，因此每個方向都會獨立排程。
5. **巢狀結構會被攤平成情境。** SR 會將上層標題與巢狀清單項目視為卡片的情境。Decks 會把這整條路徑擷取到卡片的正面——例如一個深層巢狀的 `Function :: Powerhouse` 會變成 `Cell Anatomy > Mitochondria > … > Function`——並以你所選設定檔的單一標題層級呈現（單獨的筆記標題 H1 會被略過）。透過預先安裝的 **Heading 1–6** 設定檔可以選擇任意層級。
6. **智慧自動排版會挑選最佳版面。** 簡短的單行卡片會變成精簡 **表格** 中的列（背單字時不必無止盡地捲動）；含有程式碼區塊、清單或數學式的多行卡片則會變成 **標題**，以保留其格式。你可以在對話框中將其覆寫為 *全部使用標題* 或 *全部使用表格*。
7. **整篇筆記複習也會一併遷移。** 你以整篇方式複習的筆記（`#review` 標籤）會變成 Decks 的 **標題模式** 卡片（檔名 = 正面，整篇筆記 = 背面），歸入專屬的 `…/review` 設定檔。它們的排程會從筆記的 `sr-*` frontmatter 或其檔尾標記讀取。
8. **你的排程狀態會被轉換成 FSRS-6。** Decks 會讀取舊式的 `<!--SR:-->` 中繼資料——SM-2（`due, interval, ease`）或已是 FSRS——並將其對應到穩定度／難度／到期狀態。反向卡片會維持 **兩份各自獨立的** 紀錄（閱讀與回想），與原外掛儲存的方式完全相同。
9. **每張遷移後的卡片都會寫入一筆複習日誌**，因此這些卡片一出現在 Decks 中，就已經以正確的日期與正確的間隔到期了——你是恢復進度，而非重新開始。

在對話框中選擇一個設定檔（或使用預設值）——其標題層級與排程設定會套用到遷移後的卡組。

## 從 Anki 遷移

正從 Anki 切換過來？你可以把整個收藏匯入 Decks——**不丟失卡片、媒體或複習記錄**——並繼續使用 FSRS-6 學習。

在 Anki 中，將你的牌組（或整個收藏）匯出為 **`.apkg`**（**檔案 → 匯出**，格式為 *Anki 牌組包*，勾選**包含媒體**與**包含排程資訊**）。接著從牌組面板工具列開啟匯入器，或執行 **「Import from Anki」** 命令，選擇檔案和目標資料夾並匯入。新舊（壓縮）`.apkg` 匯出皆可使用。

**你的 Anki 收藏永遠不會被更動。** 匯入是增量式的：它會把新檔案寫入你選擇的目標資料夾（巢狀於 `#decks/anki` 標籤下），並保持來源 `.apkg` 原樣。重新匯入同一檔案會覆寫它產生的檔案並重新整理其媒體——因此你隨時可以重新執行。

**運作方式**

1. **選擇 `.apkg` 和目標資料夾。** Decks 在記憶體中解壓它，讀取內嵌的 Anki 收藏（舊格式或較新的壓縮格式），並把每個引用的媒體檔案複製到你儲存庫中的 `media/` 資料夾。原始 Anki 牌組階層（`父::子`）以資料夾形式保留。
2. **每種筆記類型都變成乾淨的 Decks 卡片。** 基礎筆記會在精簡的**表格**與**標題**之間自動切換；**cloze** 挖空變為 `==…==` 標示——包括 `$…$` MathJax 內的 cloze；**多欄位／範本**筆記會取得自動產生的範本；Anki 的**圖像遮罩**卡片會轉換為 Decks 原生遮罩。
3. **媒體、公式和標籤一併遷移。** 音訊和影像會被嵌入，並在複習時播放／算繪；影像保持原始大小；LaTeX/MathJax 得以保留；你的 Anki 標籤會被分組並排序為易讀的小節。
4. **你的排程狀態會被轉換為 FSRS-6。** 每張卡片的到期日、間隔和易度——以及它完整的 Anki 複習記錄——都會對應為穩定度／難度／到期狀態並寫為複習日誌，因此卡片會**在正確的日期以正確的間隔已然到期**。你是繼續，而非重新開始。
5. **大型、媒體密集的牌組依然流暢。** 大牌組會自動拆分為受限的、依子資料夾歸類的檔案——同時依卡片數量和媒體嵌入數量——這樣即使有數千段音訊的牌組，在 Obsidian 中也能快速開啟。較小的牌組保持為單一檔案。
6. **你能看到進度。** 進度列會追蹤每個階段——讀取收藏、寫入牌組、複製媒體、同步以及匯入複習記錄——因此即便是大型匯入也絕不會看起來卡住。

在對話框中選擇一個設定檔（或使用預設）——其標題層級和排程設定會套用到匯入的牌組，這些牌組巢狀於 `#decks/anki` 標籤下。

## 版本說明與支援

- 每次更新的 **版本說明** 可以在 [`release-notes/`](./release-notes/) 中找到。
- **在 Discord 上討論** — [加入伺服器](https://discord.com/channels/686053708261228577/1497268419861418035)。
- **支持開發** — [贊助我一杯咖啡](https://www.buymeacoffee.com/dscherdil0)。
- **翻譯指南** - [翻譯指南](./docs/TRANSLATING.md)。

## AI 協助（可選）

可選的 AI 功能，**在你於「設定 → AI」中加入供應商金鑰之前都是關閉的**：

- **產生** —— 依提示（以及可選的筆記/圖片）產生卡片，並以「標題+段落」、表格或白板的形式儲存到新檔案或既有卡組。
- **重構**單張卡片或整批所選卡片，或將一張卡片**拆分**為多張 —— 每項變更在套用前都由你審閱。

自備 OpenAI、Anthropic（Claude）、Google（Gemini）或相容 OpenAI/本機端點的金鑰。金鑰儲存在本機的 `ai-keys.json`，絕不寫入 `data.json`，因此不會透過同步離開你的裝置。除非你觸發某個動作，否則不會向供應商傳送任何內容；每個請求僅包含關於 Decks 運作方式的內建提示、你的指示以及該動作的內容。

![Decks AI Generator](./decks_ai_generate.gif)

## 建構基礎

Decks 建構於 **[`@decks/core`](https://github.com/dscherdi/decks-core)** 之上 —— 實作解析、FSRS 排程、同步與 AI 編排的開源（MIT）引擎。此外掛是圍繞它的 Obsidian 專用外殼。

## 授權

本專案採用 **GNU Affero General Public License v3.0 或更新版本**（AGPL-3.0-or-later）授權。

簡而言之：你可以自由使用、修改與散布本軟體。但若你修改後散布了你的變更——或修改後透過網路向使用者提供——你必須以相同的
AGPL-3.0 授權公開你修改後的原始碼。

Copyright (C) 2026 Xherdi Lika. 完整文字請見 [LICENSE](./LICENSE) 檔案。

---

> 本翻譯為草稿 — 歡迎在 issue tracker 中提出更正與建議。

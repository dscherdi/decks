// Moved to @decks/core so the mobile app can stamp too. The Obsidian-specific
// half is ObsidianNoteAccess; everything else was already platform-agnostic.
export { AnchorStamper } from "@decks/core";
export type { StampOutcome } from "@decks/core";

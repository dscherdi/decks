# Decks ![Downloads](https://img.shields.io/github/downloads/dscherdi/decks/total) ![Downloads Latest](https://img.shields.io/github/downloads/dscherdi/decks/latest/total?style=flat-square) ![Release](https://img.shields.io/github/v/release/dscherdi/decks)

[English](./README.md) · [Deutsch](./README.de.md) · [Español](./README.es.md) · [Français](./README.fr.md) · [Italiano](./README.it.md) · [Русский](./README.ru.md) · [Türkçe](./README.tr.md) · [Shqip](./README.sq.md) · **العربية** · [हिन्दी](./README.hi.md) · [中文](./README.zh.md) · [繁體中文](./README.zh-TW.md) · [日本語](./README.ja.md)

**حول ملاحظات Obsidian الخاصة بك إلى بطاقات تعليمية (Flashcards). لا توجد صيغ معقدة. لا توجد رزم منفصلة لإنشائها.**

قم بإضافة العلامة `#decks` إلى ملف. كل عنوان من المستوى `##` يصبح واجهة البطاقة؛ والنص الذي تحته يصبح ظهر البطاقة. الجداول، وإخفاء أجزاء من الصور (image occlusion)، والفراغات (cloze) باستخدام `==التمييز==` تعمل بنفس الطريقة. تتم جدولة المراجعة بواسطة FSRS-6 — خوارزمية التكرار المتباعد الحديثة.

![Demo](./decks_showcase.gif)

[Discord](https://discord.com/channels/686053708261228577/1497268419861418035) · [ملاحظات الإصدار](./release-notes/) · [اشترِ لي قهوة](https://www.buymeacoffee.com/dscherdil0)

## لماذا تختار Decks

- **ملاحظاتك هي الرزمة نفسها.** قم بإضافة علامة للملف: كل عنوان في المستوى الذي تختاره يصبح الواجهة، والنص الذي تحته يصبح الظهر. إذا كنت قادماً من Anki، فلا داعي لكتابة الأشياء مرتين.
- **أربعة تنسيقات، لا توجد صيغ لتعلمها.** العناوين، الجداول ذات العمودين، إخفاء الصور، والفراغات `==cloze==` باستخدام أداة التمييز التي تستخدمها بالفعل.
- **وضع الامتحان.** جلسات امتحان مُقيَّمة بأسئلة الاختيار من متعدد والأسئلة الكتابية وأسئلة الفراغات (cloze).
- **جدولة أصلية باستخدام FSRS.** ثلاثة ملفات شخصية (قياسي / مكثف / مُدرب)، أهداف احتفاظ لكل علامة، لا توجد قيود من خوارزمية SM-2 القديمة.
- **ضبط الخوارزمية.** بنقرة واحدة، يقوم المحسن بتدريب أوزان FSRS بناءً على سجل مراجعتك الشخصي — جدولة أفضل تتناسب مع منحنى النسيان الخاص بك، كل ذلك محلياً على جهازك.
- **مزامنة حقيقية بين الأجهزة.** تندمج قاعدة البيانات عبر iCloud/Dropbox تلقائياً — راجع على الهاتف والكمبيوتر، دون فقدان سجل المراجعة.
- **مصمم للهواتف المحمولة.** واجهة مراجعة محسنة للمس، متوافقة مع المناطق الآمنة للشاشة، ويتم اختبارها يومياً على الهواتف.

## البدء السريع في 60 ثانية

1. قم بتثبيت **Decks** من إضافات المجتمع (Community Plugins) وقم بتمكينه.
2. افتح أي ملاحظة. أضف `#decks` في بداية الملف (frontmatter) أو كعلامة داخل النص.
3. اكتب عنواناً من المستوى `##`، ثم فقرة تحته. كرر ذلك للعدد الذي تريده من البطاقات:

   ```markdown
   ---
   tags: [decks/english]
   ---

   ## ماذا تعني "Hola" باللغة الإنجليزية؟

   Hello.

   ## كيف تقول "شكراً" بالإنجليزية؟

   Thank you.
   ```

4. انقر على **أيقونة الدماغ** في الشريط الجانبي لفتح لوحة Decks. انقر على ملفك. ابدأ المراجعة.

يصبح اسم الملف هو اسم الرزمة. تتم مزامنة البطاقات تلقائياً عند حفظ الملاحظة.

## تنسيقات البطاقات

يدعم Decks أربع طرق لإنشاء البطاقات. اختر الطريقة التي تناسب أسلوبك في كتابة الملاحظات.

<details>
<summary><b>العنوان + الفقرة</b> — التنسيق الافتراضي. كل عنوان في المستوى المُعَدّ (H2 افتراضياً) هو الواجهة، والنص تحته هو الظهر.</summary>

```markdown
---
tags: [decks/english]
---

## ماذا تعني "Hola" باللغة الإنجليزية؟

Hello.

## كيف تقول "شكراً" بالإنجليزية؟

Thank you.
```

يصبح اسم الملف هو اسم الرزمة. يمكن ضبط مستوى العنوان لكل ملف شخصي (H2 افتراضياً). العناوين الأعلى من المستوى المُعَدّ لا تتحول إلى بطاقات — بل تُحفظ كمسار تنقل (مثل `الفصل 1 > القسم 2`) يُرفق بكل بطاقة لتوفير السياق.

أضِف **ملاحظات** اختيارية إلى بطاقة العنوان+الفقرة عبر تعليق Obsidian (`%%تلميح%%`) في أي مكان من النص، أو بعد فاصل `---` في النهاية. تظهر الملاحظات عند الطلب (المفتاح **N**) أثناء المراجعة.

</details>

<details>
<summary><b>الجداول</b> — جداول Markdown ذات العمودين، مع عمود اختياري للملاحظات.</summary>

```markdown
## المفاهيم

| السؤال               | الإجابة                                     |
| -------------------- | ------------------------------------------- |
| ما هو البناء الضوئي؟ | العملية التي تستخدمها النباتات لتحويل الضوء |
| عرف الجاذبية         | القوة التي تجذب الأشياء نحو بعضها البعض     |
```

- العمود الأول = الواجهة، العمود الثاني = الظهر. يتم تجاهل صف رأس الجدول.
- يجب أن تقع الجداول مباشرة تحت العنوان (بدون أي فقرات أخرى في المنتصف).
- أضف عمود "ملاحظات" ثالثاً للتلميحات التي تظهر عند الضغط على **N** أثناء المراجعة.

</details>

<details>
<summary><b>الفراغات (Cloze)</b> — قم بتمييز النص بـ <code>==النص==</code> لإخفائه.</summary>

```markdown
## النظام الشمسي

==الشمس== هي النجم في مركز نظامنا الشمسي. أقرب كوكب هو ==عطارد==، وأكبر كوكب هو ==المشتري==.
```

كل جزء مميز يصبح بطاقة. أثناء المراجعة، يظهر الفراغ النشط كـ `[...]`؛ انقر للكشف عنه. تعمل الفراغات أيضاً داخل خلايا الجدول. ممكّن بشكل افتراضي.

</details>

<details>
<summary><b>إخفاء أجزاء من الصور (Image occlusion)</b> — أخفِ مناطق من صورة وتذكّر ما تحتها. طريقتان: تفاعلية (رسم مربعات) أو قائمة مرقمة.</summary>

**تفاعلية (مُوصى بها).** نفّذ الأمر **«إنشاء إخفاء صورة عند المؤشر»**، واختر صورة، ثم ارسم المربعات عليها مباشرةً في المحرر. اكتب إجابة بصيغة Markdown/LaTeX لكل مربع، أو اتركها فارغة لإخفاء تسمية مدمجة في الصورة فقط. تُحفظ ككتلة شيفرة `decks-occlusion` مستقلة (الإحداثيات بالنسب المئوية، لذا تتكيّف مع أي جهاز):

````markdown
```decks-occlusion
image: "[[heart.png]]"
version: 2
masks:
  - id: m1
    x: 12.5
    y: 30
    w: 18
    h: 9.5
    answer: "**البطين** الأيسر"
  - id: m2
    x: 55
    y: 22
    w: 14
    h: 8
    answer: ""
```
````

كل مربع بطاقة. أثناء المراجعة، يكون المربع مخفيًا في الواجهة ويُكشف في الظهر (مع إجابته)؛ وفي وضع القراءة ترى المخطط مُسمّى بالكامل. يمكن تعديله في أي وقت من زر **تعديل** الخاص بالكتلة أو من مدير البطاقات.

**قائمة مرقمة (بسيطة).** تعمل أيضًا صورة مُضمّنة متبوعة بقائمة مرقمة:

```markdown
## عظام الذراع

![[arm_bones.png]]

1. ==العضد==
2. ==الكعبرة==
3. ==الزند==
```

كل عنصر في القائمة بطاقة؛ تظهر الصورة في الواجهة ويُخفى العنصر المطابق في الظهر.

تعتمد كلتا الطريقتين على ميزة الفراغات (cloze)، لذا يجب تفعيل cloze في الملف الشخصي، ويجب أن تكون الكتلة تحت عنوان يُحلَّل.

</details>

### رزم Canvas

أنشئ بطاقات على ملف Obsidian Canvas (`.canvas`) بدلًا من ملف Markdown. كل canvas داخل المجلد المُعدّ يصبح رزمة؛ تُحلَّل كل عقدة نصية بنفس صيغ البطاقات الأربع المذكورة أعلاه. هيّئ ذلك في **الإعدادات → رزم Canvas**: المجلد والعلامة (الافتراضي `#decks/canvas`). "اذهب إلى المصدر" خلال المراجعة يفتح canvas ويُركّز على عقدة المصدر. عند التثبيت الأول (أو الترقية)، يُنشأ تلقائيًا ملف `Decks — البدء مع Canvas.canvas` داخل مجلد `Canvas decks/`.

**البطاقات المكانية (Spatial cards)**: اربط العُقد النصية بحوافٍ — كل حافة تصبح بطاقةً: العقدة المصدر هي الوجه (السؤال)، والعقدة الهدف هي الظهر (الجواب)، وعنوان الحافة يعمل كتلميح اختياري. تعمل السلاسل (A → B → C) وعلاقات واحد-إلى-متعدد ومتعدد-إلى-واحد جميعها؛ أما العقد غير المتصلة فلا تزال تُحلَّل بالصيغ الأربع أعلاه. التفاصيل في **[docs/CANVAS_DECKS.md](docs/CANVAS_DECKS.md)**.

![Canvas Spatial Cards Demo](./canvas_spatial_cards_demo.gif)

## القوالب

اعرض صفوف الجدول عبر تصميم بطاقة تُنشئه مرة واحدة. اكتبه بلغة HTML/CSS أو Markdown، وضع العناصر النائبة
`{{Column}}`، واربطه بجداولك عبر وسم — يطبّق قالب واحد التنسيق على كل صف مطابق.

```decks-html-front
<ruby>{{Word}}<rt>{{Reading}}</rt></ruby>
```

من **الإعدادات ← القوالب** اختر مجلدًا، ثم ضع الوسم نفسه على ملف القالب وعلى عنوان الجدول — وانتهى الأمر.
تدعم القوالب وجوه الأمام/الخلف/الملاحظات بلغة HTML أو Markdown، وتُعرض داخل بيئة معزولة ومنقّاة ومراعية للسمة،
وتوفّر متغيّرات CSS (`--padding` و`--align` و`--bg` …) للتحكم الكامل في التخطيط — من بطاقات قراءة مريحة إلى
تصاميم مخصّصة تملأ البطاقة بالكامل. أما الجداول التي لا يوجد لها قالب مطابق فتستمر في استخدام الأعمدة العادية.

راجع **[docs/TEMPLATES.md](docs/TEMPLATES.md)** للدليل الكامل والأمثلة.

## رزم الامتحانات

شغّل رزمة كامتحان مُقيَّم: مجموعة مسحوبة من الأسئلة تجيب عنها في جلسة واحدة وبأي ترتيب، مع عرض تفصيلي للنتائج في النهاية — واختياريًا حدّ زمني ودرجة نجاح. تُفعَّل الامتحانات لكل ملف تعريفي على حدة، وتضيف صيغة تأليف واحدة: عنوان تليه قائمة مهام يصبح بطاقة اختيار من متعدد.

```markdown
## أي عنصر هو غاز نبيل؟

- [ ] الأكسجين
- [x] الأرجون
- [ ] النيتروجين
```

يحدد `- [x]` الخيار الصحيح؛ وتحديد عدة خيارات يجعل السؤال متعدد التحديد.

- ضع على الملاحظة العلامة الفرعية `exams` من علامة الرزمة لديك (افتراضيًا `#decks/exams`) لاستخدام الملف التعريفي المثبَّت مسبقًا **Exams**، أو فعّل **Exam questions** في أي ملف تعريفي.
- ابدأ من قائمة الرزمة (**⋮ → Start exam**) أو بالنقر على رزمة امتحان؛ يعرض مربع حوار الإعداد عدد الأسئلة ويتيح لك ضبط إعدادات الامتحان.
- إلى جانب الاختيار من متعدد، تُطرح بطاقات العنوان-والإجابة وصفوف الجداول كأسئلة تُكتب إجابتها، وتعرض بطاقات الفراغات (cloze) الجملة مع التمييزات كفراغات تُكتب إجابتها.
- تُقيَّم الإجابات المكتوبة بدقة تامة، أو بتسامح مع الأخطاء المطبعية الصغيرة، أو بتقييم ذاتي؛ وتوجد القيم الافتراضية للامتحان (عدد الأسئلة، الحد الزمني، درجة النجاح، الخلط، توقيت التغذية الراجعة، تسميات الخيارات) في الملف التعريفي.
- تُخزَّن الامتحانات المكتملة في قاعدة بيانات الإضافة، وتندمج بين الأجهزة، وتُدرَج في النسخ الاحتياطية.

تُنشأ رزمة "Demo exam" تعرض كل صيغ الأسئلة عند التثبيت الأول (أو عبر أمر **Create demo exam deck**).

راجع **[docs/EXAM_DECKS.md](docs/EXAM_DECKS.md)** للاطلاع على قواعد التأليف الكاملة.

## جدولة مخصصة

تأتي خوارزمية FSRS بإعدادات افتراضية ممتازة تعمل بشكل رائع منذ البداية. بمجرد أن تجمع حوالي 100 مراجعة، يمكنك تدريب أوزان الخوارزمية الـ 21 على سجل مراجعتك الشخصي والحصول على جداول مخصصة لمنحنى النسيان الخاص بك — نفس ما يفعله Anki على الكمبيوتر، ولكن محلياً على جهازك، بدون خوادم أو تتبع للبيانات.

**الإعدادات → ضبط الخوارزمية → تحسين المعلمات (Optimize parameters).** يستغرق التدريب ثوانٍ للرزم العادية؛ سترى مقارنة لمقدار التحسن (log-loss) قبل وبعد. انقر على "تطبيق" لاستخدام الأوزان المُدربة.

## الانتقال من Spaced Repetition

هل تستخدم بالفعل إضافة **Spaced Repetition**؟ يمكنك الانتقال إلى Decks **دون فقدان بطاقاتك أو سجل مراجعتك** — ومتابعة مراجعاتك تمامًا من حيث توقفت.

افتح أداة الترحيل من شريط أدوات لوحة الرزم (أيقونة المكعب) أو شغّل أمر **"الترحيل من إضافة Spaced Repetition"**.

**لا يتم المساس بملاحظاتك الأصلية أبدًا.** الترحيل إضافيّ: فهو يكتب ملفات جديدة في مجلد هدف تختاره أنت (يحاكي بنيتك) ويترك ملاحظاتك المصدرية كما هي تمامًا. إعادة تشغيل أداة الترحيل تكتفي بالكتابة فوق الملفات التي ولّدتها.

**كيف تعمل**

1. **اختر مجلد المصدر** المراد فحصه (أو اتركه فارغًا لفحص الخزنة بأكملها)، ومجلد هدف للمخرجات. يعثر Decks على كل ملاحظة تحتوي على بطاقات قديمة — أحادية السطر (`Front :: Back`)، ومعكوسة (`Front ::: Back`)، ومتعددة الأسطر (`?` / `??`)، والفراغات (`==…==` / `{{…}}`) — ومراجعات الملاحظة الكاملة (العلامة `#review`).
2. **تُقسَّم كل ملاحظة إلى ملفين نظيفين.** تحتفظ **رزمة البطاقات** (`<الملاحظة> (البطاقات)`) بالبطاقات المستخرجة بصيغة Decks القياسية، وتحتفظ **الملاحظة القابلة للقراءة** بالنص — مع *إزالة تنسيق* صيغة البطاقات وتحويلها إلى نص عادي (`::` / `:::` تصبح " — "، و`?` / `??` تَصِل السؤال بالجواب، وتُستعاد فراغات `==…==` / `{{…}}` إلى جوابها). تُحترم الفواصل التي ضبطتها، فيعمل هذا حتى لو خصّصتها. لا يُحذف أي شيء — تبقى ملاحظة القراءة لديك كاملة.
3. **يُربَط الملفان تبادليًا في الـ frontmatter.** تحصل الملاحظة القابلة للقراءة على خاصية `البطاقات` تشير إلى رزمتها، وخاصية `ملاحظة المصدر` تشير إلى الأصل؛ وتحصل الرزمة على خاصية `ملاحظة المصدر` أيضًا. إذا كانت ملاحظة تستخدم بالفعل أحد أسماء هذه الخصائص، فإن أداة الترحيل تكتب فوقها بدلًا من إنشاء نسخة مكرَّرة. تُحفظ علاماتك — وتُترجَم العلامة الأساسية القديمة (مثل `#flashcards`) إلى علامة Decks التي ضبطتها.
4. **تصبح البطاقات المعكوسة بطاقتين.** تُوسَّع بطاقة `Front ::: Back` إلى بطاقة أمامية وبطاقة معكوسة في **نفس** ملف الرزمة، فيُجدوَل كل اتجاه باستقلالية.
5. **تُسطَّح البنية المتداخلة لتصبح سياقًا.** يعامل SR العناوين السلفية ونقاط القوائم المتداخلة كسياق للبطاقة. يلتقط Decks هذا المسار كاملًا في وجه البطاقة — فمثلًا تصبح `Function :: Powerhouse` المتداخلة بعمق `Cell Anatomy > Mitochondria > … > Function` — معروضة عند مستوى العنوان الواحد لملفك التعريفي (يُحذَف عنوان H1 المفرد الخاص بعنوان الملاحظة). اختر أي مستوى عبر ملفات **Heading 1–6** المثبَّتة مسبقًا.
6. **يختار التوجيه التلقائي الذكي أفضل تخطيط.** تصبح البطاقات القصيرة أحادية السطر صفوفًا في **جدول** مدمج (لا تمرير لا ينتهي للمفردات)؛ أما البطاقات متعددة الأسطر — التي تحوي كتل شيفرة أو قوائم أو معادلات — فتصبح **عناوين** كي ينجو تنسيقها. يمكنك تجاوز ذلك إلى *كل العناوين* أو *كل الجداول* في مربع الحوار.
7. **تُرحَّل مراجعات الملاحظة الكاملة أيضًا.** الملاحظات التي راجعتها ككل (العلامة `#review`) تصبح بطاقات Decks بـ **وضع العنوان** (اسم الملف = الوجه، والملاحظة كاملة = الظهر) ضمن ملف تعريفي مخصص `…/review`. يُقرأ جدولها من الـ frontmatter `sr-*` للملاحظة أو من علامة نهاية الملف.
8. **تُترجَم حالة جدولتك إلى FSRS-6.** يقرأ Decks بيانات `<!--SR:-->` القديمة — سواء SM-2 (`due, interval, ease`) أو FSRS مسبقًا — ويربطها بحالة استقرار/صعوبة/استحقاق. تحتفظ البطاقات المعكوسة بسجلَّين **منفصلين** (القراءة مقابل الاستذكار)، تمامًا كما خزّنتها الإضافة الأصلية.
9. **يُكتَب سجل مراجعة لكل بطاقة مُرحَّلة**، فبمجرد ظهور البطاقات في Decks تكون مستحقة في التاريخ الصحيح وبالفترة الصحيحة — تستأنف ولا تبدأ من جديد.

اختر ملفًا تعريفيًا في مربع الحوار (أو استخدم الافتراضي) — يُطبَّق مستوى عنوانه وإعدادات جدولته على الرزم المُرحَّلة.

## الانتقال من Anki

هل تنتقل من Anki؟ يمكنك نقل مجموعتك بالكامل إلى Decks **دون فقدان البطاقات أو الوسائط أو سجل المراجعة** — والاستمرار في الدراسة باستخدام FSRS-6.

في Anki، صدّر مجموعتك (أو المجموعة كاملة) بصيغة **`.apkg`** (**ملف → تصدير**، التنسيق *حزمة مجموعة Anki*، مع تفعيل **تضمين الوسائط** و**تضمين معلومات الجدولة**). ثم افتح أداة الاستيراد من شريط أدوات لوحة المجموعات أو شغّل الأمر **«Import from Anki»**، واختر الملف ومجلدًا هدفًا ثم استورد. تعمل صادرات `.apkg` القديمة والحديثة (المضغوطة) على حد سواء.

**لا يُمَس ملف مجموعتك في Anki أبدًا.** الاستيراد إضافي: يكتب ملفات جديدة في مجلد هدف تختاره، متداخلة تحت الوسم `#decks/anki`، ويترك ملف `.apkg` المصدر كما هو. تؤدي إعادة استيراد الملف نفسه إلى الكتابة فوق الملفات التي أنشأها وتحديث وسائطها — لذا يمكنك تشغيله في أي وقت.

**كيف يعمل**

1. **اختر ملف `.apkg` ومجلدًا هدفًا.** يفك Decks ضغطه في الذاكرة، ويقرأ مجموعة Anki المضمَّنة (التنسيق القديم أو الجديد المضغوط)، وينسخ كل ملف وسائط مُشار إليه إلى مجلد `media/` في خزينتك. يُحفظ تسلسل مجموعات Anki الأصلي (`أصل::فرع`) على شكل مجلدات.
2. **يتحول كل نوع ملاحظة إلى بطاقة Decks نظيفة.** تتنقل الملاحظات الأساسية تلقائيًا بين **جدول** مدمج و**عناوين**؛ وتتحول فراغات **cloze** إلى تظليلات `==…==` — بما في ذلك cloze داخل MathJax بصيغة `$…$`؛ وتحصل الملاحظات **متعددة الحقول / ذات القالب** على قالب مُولَّد تلقائيًا؛ وتأتي بطاقات **حجب الصور** من Anki كحجب أصلي في Decks.
3. **تُنقل الوسائط والرياضيات والوسوم.** يُضمَّن الصوت والصور ويُشغَّلان/يُعرضان أثناء المراجعة؛ وتحتفظ الصور بحجمها الأصلي؛ ويُحفظ LaTeX/MathJax؛ وتُجمَّع وسوم Anki وتُرتَّب في أقسام قابلة للقراءة.
4. **تُترجَم حالة الجدولة إلى FSRS-6.** تُربَط تاريخ الاستحقاق والفاصل والسهولة لكل بطاقة — إضافةً إلى كامل سجل مراجعتها في Anki — بحالة استقرار/صعوبة/استحقاق وتُكتَب كسجل مراجعة، فتظهر البطاقات **مستحقة بالفعل في التاريخ الصحيح بالفاصل الصحيح**. أنت تستأنف ولا تبدأ من جديد.
5. **تبقى المجموعات الكبيرة الغنية بالوسائط سلسة.** تُقسَّم المجموعة الكبيرة تلقائيًا إلى ملفات محدودة موزَّعة في مجلدات فرعية — حسب عدد البطاقات وعدد تضمينات الوسائط — حتى تفتح مجموعة بها آلاف المقاطع الصوتية بسرعة في Obsidian. وتبقى المجموعات الأصغر ملفًا واحدًا.
6. **تشاهد ما يحدث.** يتتبع شريط التقدم كل مرحلة — قراءة المجموعة، وكتابة المجموعات، ونسخ الوسائط، والمزامنة، واستيراد سجل المراجعة — حتى لا يبدو الاستيراد الكبير عالقًا أبدًا.

اختر ملفًا شخصيًا في مربع الحوار (أو استخدم الافتراضي) — يُطبَّق مستوى العنوان وإعدادات الجدولة الخاصة به على المجموعات المستوردة، المتداخلة تحت الوسم `#decks/anki`.

## ملاحظات الإصدار والدعم

- توجد **ملاحظات الإصدار** لكل تحديث في [`release-notes/`](./release-notes/).
- **ناقش على Discord** — [انضم إلى الخادم](https://discord.com/channels/686053708261228577/1497268419861418035).
- **ادعم التطوير** — [اشترِ لي قهوة](https://www.buymeacoffee.com/dscherdil0).
- **دليل الترجمة** - [دليل الترجمة](./docs/TRANSLATING.md).

## مساعدة الذكاء الاصطناعي (اختيارية)

ميزات ذكاء اصطناعي اختيارية، **معطّلة حتى تضيف مفتاح مزوّد في الإعدادات ← الذكاء الاصطناعي**:

- **التوليد** — أنشئ بطاقات من موجّه (مع ملاحظات/صور اختيارية) واحفظها في ملف جديد أو مجموعة موجودة بصيغة عنوان+فقرة أو جدول أو لوحة.
- **إعادة الصياغة** لبطاقة أو لتحديد كامل، أو **التقسيم** لبطاقة إلى عدة بطاقات — تراجع كل تغيير قبل تطبيقه.

استخدم مفتاحك الخاص لـ OpenAI أو Anthropic (Claude) أو Google (Gemini) أو نقطة نهاية متوافقة مع OpenAI/محلية. تُحفظ المفاتيح محليًا في `ai-keys.json` ولا تُكتب أبدًا في `data.json`، لذا لا تغادر جهازك عبر المزامنة. لا يُرسَل شيء إلى المزوّد ما لم تبدأ إجراءً؛ ويحتوي كل طلب فقط على موجّه مدمج حول كيفية عمل Decks وتعليماتك ومحتوى ذلك الإجراء.

![Decks AI Generator](./decks_ai_generate.gif)

## مبني على

‏Decks مبني على **[`@decks/core`](https://github.com/dscherdi/decks-core)** — المحرك مفتوح المصدر (MIT) الذي يطبّق التحليل وجدولة FSRS والمزامنة وتنسيق الذكاء الاصطناعي. الإضافة هي الغلاف الخاص بـ Obsidian حوله.

## الترخيص

هذا المشروع مرخّص بموجب **GNU Affero General Public License v3.0 أو أحدث** (AGPL-3.0-or-later).

باختصار: أنت حر في استخدام هذا البرنامج وتعديله وتوزيعه. ومع ذلك، إذا عدّلته ووزّعت تغييراتك — أو عدّلته
وقدّمته للمستخدمين عبر شبكة — فيجب أن تجعل شيفرتك المصدرية المعدّلة متاحة للعموم بموجب الترخيص نفسه
AGPL-3.0.

Copyright (C) 2026 Xherdi Lika. راجع ملف [LICENSE](./LICENSE) للنص الكامل.

---

> هذه الترجمة عبارة عن مسودة — نرحب بالتصحيحات والاقتراحات عبر مُتتبّع المشكلات (issues).

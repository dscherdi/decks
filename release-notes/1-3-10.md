### Changes

**Database**
- Fixed SQL.js WASM loading: MainDatabaseService now uses embedded WASM instead of fetching from external CDN (which returned 404)

**Documentation**
- Updated README to be user-facing only with current feature set
- Added `/release` command for automated release workflow

### Changes

**Profiles**
- Fixed profile switching not persisting when changing a tag's profile
- Profile re-resolution during sync ensures decks get updated when tag mappings change
- Database schema v9: enforces one profile per tag

**UI**
- Config and profile modals now have scrollable content with fixed footer buttons
- Moved Save/Delete buttons to the fixed footer in profile manager modal
- Fixed AnkiExportModal sizing not applied on desktop
- Added missing resize handler to profile manager modal

**Performance**
- No full vault sync after completing a review session
- No full vault sync after saving profile or config changes
- Removed duplicate sync on plugin startup
- Deferred UI refresh on modal close to avoid blocking click handlers

### Changes

**Settings**

- The deck tag is now configurable in plugin settings (under Parsing). New installations default to `#decks`, while existing users keep `#flashcards` until they change it
- Changing the deck tag automatically migrates profile-tag mappings to use the new tag

**UI**

- Filter placeholder and empty-state help text now reflect the configured deck tag

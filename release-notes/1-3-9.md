### Changes

**Deck profiles**
- Profiles are now applied to tags only, not individual files
- Per-deck daily limits enforced in tag group reviews (each deck gets its own new/review quota)
- Fixed session goal showing raw card total instead of respecting limits
- Fixed limit bars not appearing for tag groups in the deck list

**FSRS algorithm**
- Fixed intensive profile bug where cards would not become due until the next day

**UI**
- Reduced excessive button padding on mobile review screen
- Centralized refresh functionality across the deck list
- Added "Configure profile" option to deck and tag group dropdown menus

**Infrastructure**
- Release notes now sourced from `release-notes/` folder per version
